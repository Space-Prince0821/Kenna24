<%= form_with(model: comment) do |form| %>
  <% if comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
        <% comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "Add your comment lob!", style: "display: block; padding-bottom: 3px; margin-top:5px; color: white; text-align:center;" %>
    <%= form.text_field :message, style:"margin-bottom:5px; width:200px;" %>
  </div>

  <div class="text-center">
    <%= form.submit %>
  </div>
<% end %>
